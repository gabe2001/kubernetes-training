apiVersion: v1
kind: Service
metadata:
  labels:
    run: nginxc-purple
  name: nginxc-purple
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: nginxc-purple
  type: NodePort
